// ------------------------------------------------------------
// Root Layout — FYTRUP Alpha12
// Ensures the app is NOT prerendered and loads as an SPA.
// ------------------------------------------------------------

export const prerender = false;
